# Discord Developer Portal에서 발급받은 값
DISCORD_TOKEN=
DISCORD_CLIENT_ID=
# 슬래시 커맨드를 바로 반영해서 테스트하고 싶으면 서버(길드) ID를 지정 (없으면 전역 등록, 반영까지 최대 1시간)
DISCORD_GUILD_ID=

# DevLog AI 백엔드 주소 (기본값 그대로 두면 됨)
DEVLOG_API_BASE=http://localhost:3000
